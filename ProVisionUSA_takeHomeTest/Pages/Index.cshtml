@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<!-- From the corresponding cs file, print out both the program name and its version in HTML -->
@foreach (var p in Model._programLister.GetProgramList())
{
    <div>[@p.programName, @p.programVersion]</div>
}